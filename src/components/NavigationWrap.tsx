import styled from "styled-components";

const NavigationDiv = styled.div<{ $bgColor?: string }>`
  display: flex;
  align-content: space-around;
  justify-content: center;
  padding: 64px 0;

  width: 100%;
`;


export default NavigationDiv;
