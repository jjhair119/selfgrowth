import styled from "styled-components";

const Weather = styled.button`
  border: none;
  background: white;
  height: 28px;
    width: 28px;
  border-radius: 8px;
  flex-shrink: 0;
    padding:0 1px;
`;

export default Weather;
