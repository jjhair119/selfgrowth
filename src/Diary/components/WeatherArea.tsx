import styled from "styled-components";

const WeatherArea = styled.div`
  width: 164px;
  flex-grow: 1;

  color: #000;
  font-size:18px;
  font-style: normal;
  font-weight: 800;
  line-height: normal;
`;

export default WeatherArea;
