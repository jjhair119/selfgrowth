import styled from "styled-components";

const WeatherArea = styled.div`
  position: absolute;
  top: 5%;
  left: 75%;

  width: 178px;
  height: 24px;
  flex-shrink: 0;

  color: #000;
  font-family: Inter;
  font-size: 20px;
  font-style: normal;
  font-weight: 800;
  line-height: normal;
`;

export default WeatherArea;
